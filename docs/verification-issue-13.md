# #13 开发入口验证

状态：开发入口切片已验收；不代表完整 Spec 001 或生产部署完成。

需求：[Spec 001](specs/001-desktop-text-loop.md)；运行方式：[独立宿主入口](host-ingress.md)。

## 已验证

- 独立 Caddy 入口仅提供 TCP 443，禁用 HTTP 跳转和 HTTP/3；可信证书通过 TLS-ALPN-01 签发，不依赖端口 80。
- 页面、API、热更新资源与 WSS 握手受独立 Basic Auth 保护；无/错误凭证拒绝，正确凭证放行。转发移除外层 Authorization，覆盖来源头；应用仅信任精确代理地址。
- 前后端没有宿主端口映射，使用隔离开发挂载；内部诊断路径不公开。
- 隔离 HTTPS 测试验证认证、代理头及升级行为；真实域名测试验证证书、页面/API 和 WSS 连接。
- 操作者使用稳定版桌面 Chrome 确认访问成功，并从外部网络验证 TCP 443 可达，80/5173/8080 不可达。仅代表该次测试路径及所测端口。
- 检查的运行日志未发现本次外层凭证，开发配置与数据被 Git 排除。
- 本地 Rust 格式/Clippy/测试、前端 lint/类型检查/构建、Playwright 流程及隔离容器冒烟通过。首次浏览器测试缺少构建产物，按开发指南先构建后通过；未削弱断言。

## 边界

部署操作已获授权，未修改无关服务、生产入口、云规则或宿主防火墙；未初始化真实业务账户或删除持久数据。云规则未直接审阅，外部探测不冒充云配置审计。未实测到期续签、完整业务组合或真实剪贴板权限；后续路由变化仍须在阶段验收回归。

公开记录只保留项目相关结论。具体主机地址、网络拓扑、无关软件、个人设备信息和私有运行配置不属于公开验收材料。CI 与合并状态以 PR 检查为准。
