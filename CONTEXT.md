# FileHop 领域术语

FileHop 是面向个人的跨设备文本与文件交换工具。所有客户端共享服务器上的私人消息流，文件交换不等同于目录同步或长期归档。

## Language

**消息流**：
单个用户在所有已登录客户端中共同查看的消息历史，不区分接收设备。
_Avoid_：会话列表、设备收件箱

**文本消息**：
消息流中的一条纯文本记录，可包含文字、URL、命令或代码片段。
_Avoid_：富文本文档

**文件消息**：
描述一次成功文件发送的历史记录，包含文件名称、大小等信息。服务器文件删除后，该记录仍保留，并标记文件已删除。
_Avoid_：文件实体

**服务器文件**：
由 FileHop 接收并保存在服务器上的文件实体。删除它会阻止所有客户端后续从服务器下载，但不影响已下载的本地副本。
_Avoid_：VPS 任意文件、文件消息

**本地副本**：
某设备已经下载保存的文件，独立于服务器文件及其他设备上的副本。删除本地副本不影响服务器文件或其他设备。
_Avoid_：同步文件、缓存

**本地下载**：
Android 应用保存且有访问权限的本地副本集合，不代表设备上的全部文件。
_Avoid_：手机文件系统

**来源标签**：
标识消息发送来源的可读名称，保留发送时的值。它不是物理设备身份，也不用于认证或授权。
_Avoid_：设备指纹、设备凭证

**草稿**：
用户尚未发起发送的文本内容，不是共享消息流中的记录。
_Avoid_：待送达消息

**发送尝试**：
一次逻辑发送操作，其类型、载荷及来源标签固定；同次重试仍属于该操作，而不是新的发送。文本载荷是正文，文件载荷包含约定的文件内容与元数据。
_Avoid_：文件内容去重

**文件传输尝试**：
一次文件发送中从头传输完整文件的尝试；一次逻辑发送可包含多个先后进行的传输尝试，但最多产生一条成功消息。
_Avoid_：分片、续传

**发送标识**：
用于辨认同一次发送尝试的唯一标识，不是消息内容摘要，也不属于某次登录。
_Avoid_：会话凭证

**结果未确认**：
客户端尚不能判断一次发送是否已由服务器保存的状态，不等于发送失败或消息不存在。
_Avoid_：未送达、确定失败

**发送成功**：
服务器已可靠保存本次发送内容及其消息记录的确认。不表示任何其他客户端已经接收、下载或阅读。
_Avoid_：已送达、已读

**文件存储额度**：
FileHop 服务器文件可使用的容量边界，不是整台服务器的硬盘容量。
_Avoid_：VPS 硬盘上限
